def encoding_fortune():
    import logging

    logger = logging.getLogger(__name__)

    logger.info(
        "🍀 UTF-8 is the language of clean repos. May your commits be crisp and your diffs be drama-free."
    )
    logger.info(
        "🍀 A well-encoded file is like a well-tended garden: it flourishes and invites collaboration."
    )
